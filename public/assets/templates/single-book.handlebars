<div id="book-wrapper">
    <h1 id="book-title" data-id={{_id}}>{{{title}}}</h1>
    <table class="table">
        <tr>
            <td col-md-6>
                <img class="img-responsive" src="{{coverUrl}}">
            </td>
            <td col-md-6>
                <p>Summary: {{description}}</p>
            </td>
        </tr>
        <tr>
            <td>
                <h4>Author: <strong>{{author}}</strong></h4>
                <h4>Pages: 
                    {{#if pages}}
                        <strong>{{pages}}</strong>                    
                    {{else}}
                        n/a
                    {{/if}}
                </h4>                
                <h4>Rating: 
                    {{#if rating}}
                        {{#each rating}}
                        <span class="star glyphicon glyphicon-star"></span>
                        {{/each}}
                    {{else}}
                        No rating
                    {{/if}}
                </h4>
                {{#if status}}
                    <div class="rating-adder">
                        <h4>Rate it: 
                                <button data-id="1" class="btn-star btn-add-rating">
                                    <span class="star glyphicon glyphicon-star"></span>
                                </button>
                                <button  data-id="2" class="btn-star btn-add-rating">
                                    <span class="star glyphicon glyphicon-star"></span>
                                </button>
                                <button  data-id="3" class="btn-star btn-add-rating">
                                    <span class="star glyphicon glyphicon-star"></span>
                                </button>
                                <button  data-id="4" class="btn-star btn-add-rating">
                                    <span class="star glyphicon glyphicon-star"></span>
                                </button>
                                <button  data-id="5" class="btn-star btn-add-rating">
                                    <span class="star glyphicon glyphicon-star"></span>
                                </button>                     
                            </button>
                        </h4>
                    </div>
                {{/if}}
                
            </td>
            <td>
                Genres:
                {{#genres}}
                <a class="btn btn-default" href="#/books-result?genre={{this}}">{{this}}</a>
                {{/genres}}
            </td>
        </tr>
        <tr>
            <td class="col-md-3">
                <div class="status-wrapper">
                    {{#if status}}<h2>Status:</h2><button class="btn btn-lg btn-unactive"><h3>{{status}}</h3></button></div>{{/if}}
                </div>
            </td>
            <td class="col-md-9">
                <div class="review-wrapper"> 
                    <h2>Reviews:</h2>
                    {{#if reviews}}
                    {{#reviews}}
                    <div>
                    <strong><a href="">{{this.nickName}}:</a></strong>        
                    <h3>{{this.review}}</h3>
                    <br>
                    <br>
                    </div>
                    {{/reviews}}
                    {{else}}
                    <h3>Yours will be the first.</h3>
                    {{/if}}
                    <div class="form-group">
                    <textarea type="text" class="form-control"  placeholder="Write something about it..."></textarea>
                    <button class="form-control">Send</button>
                    </div>
                    </div>
            </td>
        </tr>
    </table>
    
</div>